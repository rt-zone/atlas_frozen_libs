These are the files for building the atlas firmware. Put the py files in the modules folder in /ports/rp2. Put the cmodules in /ports/rp2.

Build command: make BOARD=RPI_PICO_W USER_C_MODULES=cmodules
